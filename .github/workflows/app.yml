name: Flutter app

on:
  pull_request:
    paths:
      - "app/**"
      # The end-to-end job plays against the server, and the Dart word rules
      # share server/internal/game/testdata/word_rules.json.
      - "server/**"
      - ".github/workflows/app.yml"
  push:
    branches: [main]
    paths:
      - "app/**"
      # The end-to-end job plays against the server, and the Dart word rules
      # share server/internal/game/testdata/word_rules.json.
      - "server/**"
      - ".github/workflows/app.yml"

permissions:
  contents: read

jobs:
  test:
    name: test (${{ matrix.name }})
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        include:
          # Must match the minimum in app/pubspec.yaml.
          - name: minimum 3.27.0
            flutter-version: 3.27.0
          - name: latest stable
            flutter-version: ""
            format: true
    defaults:
      run:
        working-directory: app
    steps:
      - uses: actions/checkout@v4
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          flutter-version: ${{ matrix.flutter-version }}
          cache: true
      - run: flutter --version
      - run: flutter pub get
      # One formatter version is authoritative so the jobs cannot disagree.
      - if: matrix.format
        run: dart format --output=none --set-exit-if-changed lib test
      - run: flutter analyze
      - run: flutter test

  e2e:
    name: end-to-end against the server
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-go@v5
        with:
          go-version-file: server/go.mod
          cache-dependency-path: server/go.sum
      - uses: subosito/flutter-action@v2
        with:
          channel: stable
          cache: true
      - name: Start the server with the development content policy
        working-directory: server
        run: |
          go build -o "$RUNNER_TEMP/imposter-server" ./cmd/server
          PORT=18080 IMPOSTER_DEV_POLICY=1 "$RUNNER_TEMP/imposter-server" > "$RUNNER_TEMP/server.log" 2>&1 &
          for i in $(seq 1 50); do curl -sf localhost:18080/healthz && exit 0; sleep 0.2; done
          cat "$RUNNER_TEMP/server.log"
          exit 1
      - run: flutter pub get
        working-directory: app
      - run: flutter test test/e2e
        working-directory: app
        env:
          IMPOSTER_E2E_SERVER: http://localhost:18080
      - if: failure()
        run: cat "$RUNNER_TEMP/server.log"
