<?xml version="1.0" encoding="utf-8"?>
<!-- Android 12+. The preferences file holds the guest session token; a copy on
     a second device would make both phones the same player and take the
     connection from each other. Stats and settings are device-local anyway. -->
<data-extraction-rules>
    <cloud-backup>
        <exclude domain="sharedpref" path="FlutterSharedPreferences.xml" />
    </cloud-backup>
    <device-transfer>
        <exclude domain="sharedpref" path="FlutterSharedPreferences.xml" />
    </device-transfer>
</data-extraction-rules>
