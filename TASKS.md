# משימות הפרויקט

עודכן לאחרונה: 15 בספטמבר 2026

הקובץ הזה הוא מקור המעקב המרכזי אחר העבודה על `מי המתחזה?`. חוקי המוצר וההחלטות המלאות נשארים במסמכי `docs/`.

## כללי מעקב

- `[x]` — הושלם.
- `[ ]` — פתוח.
- משימה שמסומנת **חסום** אינה מתחילה עד להשלמת התלות הרשומה.
- בכל השלמת משימה יש לסמן אותה כאן באותו commit.
- החלטת מוצר חדשה מתועדת גם ב־`docs/decisions.md` ובמסמך המקצועי המתאים.
- אין להתחיל משימות של מצב שאלות לפני השלמת ה־MVP של מצב המילים.

## אבן הדרך הנוכחית

**השלמת ה־MVP.** משחק ברשת וחדר פרטי פועלים מקצה לקצה בשרת ובאפליקציה. נשארו: מילון תוכן לא ראוי (בלעדיו השרת מתחיל משחקים רק במצב פיתוח), שמירת ניצחונות והפסדים, הפונטים, build ל־iOS, מזהה אפליקציה סופי, בחירת Hosting והשלמת Design V1.

## הושלם

### אפיון מוצר

- [x] נקבע שם האפליקציה: `מי המתחזה?`.
- [x] הוגדר היקף ה־MVP: מצב מילים, משחק ברשת וחדרים פרטיים.
- [x] הוחלט שאין חשבון, צ'אט חופשי, דיווחים או ניקוד מספרי ב־MVP.
- [x] הוגדרו חוקי הרמזים, התגובות, ההצבעה וניחוש המתחזה.
- [x] הוגדרו תנאי הניצחון ושמירת ניצחונות והפסדים בלבד.
- [x] הוגדר Matchmaking של 4–8 שחקנים: 30 שניות מהשחקן הרביעי, 5 שניות מהשישי, ו־`לא נמצא משחק מתאים` אחרי 2 דקות.
- [x] הוגדרו חדרים פרטיים, נעילת הגדרות, הסרת שחקן והעברת מנהל.
- [x] הוגדרו ניתוקים, חיבור מחדש, נטישה, קריסת שרת ומקרי קצה מרכזיים.
- [x] נכתבו מסמכי האפיון, ההחלטות והשאלות הפתוחות תחת `docs/`.

### UX ועיצוב

- [x] הוגדרו 29 מסכים ומצבי מערכת.
- [x] נוצר Wireframe מלא ואינטראקטיבי לכל 29 המצבים.
- [x] נקבעה שפה חזותית בלשית, צבעונית ושובבה.
- [x] נכתב `CLAUDE.md` עם הנחיות מלאות לעיצוב.
- [x] נוצרה טיוטת High Fidelity ראשונה של 29 המסכים בקלוד.
- [x] נוצרה טיוטת Design System ראשונה בקלוד.
- [x] נוצרו 12 אווטארים מגוונים: 6 בנות ו־6 בנים.
- [x] נוצרו 11 אילוסטרציות למסכים המרכזיים ולמצבי קצה.
- [x] כל נכסי האיור נשמרו בפורמט WebP שקוף תחת `assets/`.

### תכנון טכני ומנוע משחק

- [x] נקבעו Flutter לאפליקציה, Go לשרת ו־monorepo.
- [x] נכתב מסמך ארכיטקטורה: `docs/architecture.md`.
- [x] הוגדרו חוזי REST ו־WebSocket: `docs/protocol.md`.
- [x] נוצר שלד שרת Go עם `GET /healthz` תחת `server/`.
- [x] מומש מנוע משחק ראשון ב־`server/internal/game`, ללא תלות ב־HTTP או במסד נתונים: חשיפת תפקיד, תורות אקראיים, טיימרים, רמזים, תגובות, הצבעה, הצבעה חוזרת, ניחוש, ניתוקים, יציאה ותוצאות.
- [x] נוספו Unit Tests למעברי המשחק ולחוקים המאושרים.
- [x] נוסף GitHub Actions לשרת: gofmt, go vet, golangci-lint ובדיקות עם `-race`.
- [x] שאלות שעלו בתכנון המנוע נוספו ל־`docs/open-decisions.md`.

### שרת

- [x] כללי השוואת מילים בעברית במנוע ובאפליקציה, עם מקרי בדיקה משותפים ב־`word_rules.json`.
- [x] State Machine של חדר פרטי ב־`server/internal/room`: קוד, הגדרות וננעלות, הסרה, התחלת משחק, העברת מנהל ומשחק נוסף.
- [x] REST ב־`server/internal/api`: sessions אורח, קטגוריות, תגובות, יצירת חדר והצטרפות.
- [x] WebSocket לחדרים פרטיים ולמשחקים: מעטפת v1, idempotency, ping, חיבור מחדש, Snapshots מסוננים וטיימרים סמכותיים.
- [x] מנת תוכן ראשונה ורשימת תגובות ב־`server/internal/content`.

### אפליקציה

- [x] Prototype לחיץ ב־Flutter עם נתוני דמה לזרימת משחק ברשת ולחדר פרטי, כולל widget tests ברוחב 320px ו־CI על Flutter 3.27.0 ועל stable. — הוחלף באפליקציה המחוברת לשרת.
- [x] משחק ברשת, חדרים פרטיים ומשחקים מחוברים לשרת: שכבת נתונים על `dart:io`, `GameSession` עם חיבור מחדש, מסכי חיפוש, חדר ומשחק חיים, מעטפות Android/iOS ובדיקות End-to-End מול השרת.

## P0 — השלמת Design V1

- [ ] לשלב את 12 האווטארים מתוך `assets/avatars/` בכל 29 המסכים.
- [ ] לשלב את 11 האילוסטרציות מתוך `assets/illustrations/` לפי המיפוי ב־`assets/README.md`.
- [ ] להסיר את כל ה־placeholders של דמויות ואילוסטרציות.
- [ ] להשאיר placeholder זמני ומעוצב בלבד לאיורי הקטגוריות.
- [ ] להשלים את כל מצבי הרכיבים ב־Design System: default, pressed, selected, disabled, loading, error ו־disconnected.
- [ ] לבדוק שכל 29 המסכים תואמים למסמכי המוצר ולנוסחי הפעולות המאושרים.
- [ ] לבדוק RTL מלא, safe areas ואזורים לחיצים נוחים.
- [ ] לבדוק שאין טקסט חתוך ברוחב 320px ובמסך 390×844.
- [ ] לוודא שבכל מסכי המשחק הטיימר בעיגול משמאל למעלה והיציאה מימין למעלה.
- [ ] להוסיף טיימר של 10 שניות בעיגול למסכי חשיפת התפקיד (7–8). — קיים באפליקציה.
- [ ] ליצור Prototype לחיץ לזרימת משחק ברשת. — האפליקציה עצמה מממשת את הזרימה; Prototype העיצוב עדיין פתוח.
- [ ] ליצור Prototype לחיץ לזרימת חדר פרטי. — האפליקציה עצמה מממשת את הזרימה; Prototype העיצוב עדיין פתוח.
- [ ] לחבר ב־Prototype את מסלולי השגיאה המרכזיים: כינוי חסום, רמז חסום, אין התאמה, ניתוק, הוצאה וקריסת שרת. — באפליקציה קיימים כולם מלבד כינוי חסום, שממתין למילון.
- [ ] לבצע סקירת עיצוב סופית ולרכז תיקונים.
- [ ] לאשר ולהקפיא Design V1.
- [ ] לשמור קישור לקובצי העיצוב ול־Prototype בתוך הפרויקט.
- [ ] להשלים לוגו סופי, App Icon ו־Splash Screen בהתאם לשפה המאושרת.

## P0 — החלטות מוצר שצריך לסגור לפני מימוש הפיצ'רים הרלוונטיים

### תוכן וקטגוריות

- [ ] לקבוע רשימה סופית של קטגוריות. — מנה ראשונה אושרה: אוכל, חיות, ספורט, מקצועות, מקומות, חפצים.
- [ ] לקבוע כמה מילים נדרשות בכל קטגוריה להשקה. — במנה הראשונה 20 מילים לקטגוריה.
- [x] להגדיר מבנה נתונים למילה ולשיוך שלה לקטגוריות. — `server/internal/content`: מזהה, שם ומילים לכל קטגוריה.
- [x] ליצור מאגר מילים ראשוני. — 120 מילים ב־`server/internal/content`.
- [ ] לבצע בדיקת איכות אנושית למילים: עברית, כפילויות, רמת קושי והתאמה לקטגוריה.
- [x] לקבוע רשימה מדויקת של אימוג'ים והודעות מובנות. — 😂 🤔 👀 😮 👏 🙄 ו־`רמז טוב!`, `זה מחשיד`, `לא השתכנעתי`, `מה הקשר?`.
- [ ] **חסום עד לקביעת רשימת הקטגוריות:** ליצור איור קטן ושקוף לכל קטגוריה.
- [ ] לשלב את איורי הקטגוריות בכרטיסי הקטגוריה וב־Design System.

### Matchmaking

- [x] לקבוע כמה זמן מחכים לפני הצגת הודעת `לא נמצא משחק מתאים`. — 2 דקות עם פחות מ־4 שחקנים.
- [x] לקבוע איך משתלבות ספירת 5 השניות וההמתנה של 30 השניות. — 30 שניות מהרביעי, 5 שניות מהשישי (גם ב־8), ביטול בזמן הספירה אינו עוצר אותה, ומתחת ל־4 מתחילים מחדש.

### אימות רמזים וכינויים

- [ ] ליצור מילון מילים וביטויים לא ראויים.
- [x] להחליט כיצד מנרמלים עברית לפני בדיקת רמז. — ניקוד, סימנים ואותיות סופיות (`docs/decisions.md`); נרמול כינויים ייקבע עם כללי הכינוי.
- [ ] להחליט אם אותיות שימוש והטיות נחשבות לחשיפת המילה. — אותיות שימוש הוחלטו; הטיות סוף מילה פתוחות.
- [x] להחליט כיצד מטפלים בשגיאות כתיב הקרובות למילה הסודית. — כתיב מדויק; שגיאות כתיב אינן נחשבות לאותה מילה.
- [x] להחליט כיצד משווים רמזים כדי לזהות רמז כפול. — נרמול, אותיות שימוש וכתיב מדויק; הטיות סוף מילה עדיין פתוחות.
- [ ] להגדיר מקרי בדיקה מאושרים לכללי החסימה. — מקרי בדיקה למילה סודית, רמז כפול וניחוש קיימים ב־`server/internal/game/testdata/word_rules.json` וממתינים לאישור; מקרים לתוכן לא ראוי תלויים במילון.

### שמירה ותשתית

- [ ] לקבוע כמה זמן חדר פרטי ריק נשמר לפני סגירה.
- [ ] להחליט אם ניצחונות והפסדים נשמרים רק במכשיר או באמצעות מזהה אורח אנונימי.
- [ ] לאשר נוסח סופי להודעת קריסת שרת.

## P1 — תכנון טכני

- [x] לבחור סופית טכנולוגיית Client ל־Android ול־iPhone. — Flutter.
- [ ] לבחור טכנולוגיית Backend וספק Hosting. — Go נבחר; ספק Hosting פתוח.
- [x] להחליט אם הקוד ינוהל כ־monorepo או במספר מאגרים. — monorepo.
- [x] ליצור מבנה פרויקט ראשוני ל־Client ול־Backend. — השרת ובסיס Flutter נוצרו; מעטפות Android/iOS וחיבור האפליקציה לשרת עדיין פתוחים.
- [x] לכתוב מסמך ארכיטקטורה קצר עם גבולות השירותים והאחריות שלהם.
- [x] להגדיר State Machine שרתית לכל שלבי המשחק והמעברים ביניהם.
- [x] להגדיר API ופרוטוקול Realtime בין האפליקציה לשרת.
- [x] להגדיר Event schemas עם versioning ו־idempotency.
- [ ] להגדיר מודל נתונים לשחקן אורח, משחק, חדר, רמז, תגובה, הצבעה ותוצאה. — אובייקטי הפרוטוקול הוגדרו; מודל אחסון תלוי בהחלטת השמירה.
- [x] לתכנן Matchmaking לפי שילוב קטגוריות וקיבולת. — שחקנים מחפשים יחד כשיש להם קטגוריה משותפת, והמילה נבחרת מהחיתוך (`docs/architecture.md`).
- [x] לתכנן Reconnection ושחזור מצב משחק.
- [x] לתכנן טיימרים סמכותיים בשרת וסנכרון זמן ללקוח.
- [x] לתכנן מניעת שליחה כפולה של רמזים והצבעות.
- [x] לתכנן מנגנון ניהול חדר והעברת מנהל. — `docs/architecture.md`, מומש ב־`server/internal/room`.
- [ ] לתכנן טעינה ועדכון של מאגר התוכן בלי פרסום גרסת אפליקציה חדשה. — הקטגוריות והתגובות מוגשות מהשרת (`GET /v1/categories`, `GET /v1/reactions`), כך שעדכון דורש פריסת שרת בלבד; עדכון בלי פריסה פתוח.
- [ ] להגדיר Rate limiting, הגנת Abuse ואבטחת קוד חדר. — קיימות רק מגבלת גוף בקשה והודעה של 64KB וניתוק לקוח איטי.
- [ ] לבחור Analytics, Crash Reporting, Logging ו־Monitoring.
- [ ] להגדיר סביבות development, staging ו־production. — בפיתוח `IMPOSTER_DEV_POLICY=1` מאפשר משחק בלי מילון תוכן לא ראוי, והאפליקציה מקבלת כתובת שרת ב־`--dart-define=IMPOSTER_SERVER`.
- [x] להקים CI לשרת: formatting, lint, vet ובדיקות.
- [ ] להוסיף ל־CI build, lint ובדיקות לאפליקציית Flutter. — format, analyze ו־widget tests על Flutter 3.44.0 ועל stable, build של Android ושל iOS (סימולטור) על 3.44.0, ובדיקת End-to-End מול השרת.

## P1 — מימוש MVP

### תשתית אפליקציה

- [ ] ליצור פרויקט אפליקציה עובד ל־Android ול־iPhone. — מעטפות Android ו־iOS נוצרו עם המזהה הזמני `com.example.imposter_il`; build של Android debug ושל iOS לסימולטור רץ ב־CI על Flutter 3.44.0; בדיקה על מכשירים עדיין פתוחה.
- [x] לממש Design Tokens ורכיבי בסיס מתוך ה־Design System.
- [ ] להוסיף את הפונטים Secular One ו־Rubik.
- [x] לחבר את האווטארים והאילוסטרציות לניהול Assets.
- [x] לממש בסיס ניווט, RTL, safe areas ומסכי שגיאה. — חיבור מחדש, הוצאה, תקלה בשרת ו־`לא נמצא משחק מתאים`.
- [x] לממש שמירת זהות אורח מקומית. — `shared_preferences`; session שהשרת איבד נוצר מחדש עם אותו כינוי ואווטאר.

### כניסה ומסכי מעטפת

- [x] לממש כניסה ראשונה, בחירת אווטאר וכינוי. — `POST /v1/sessions` מהאפליקציה, עם שגיאות שרת.
- [ ] לממש חסימת כינוי לא ראוי.
- [x] לממש מסך בית.
- [x] לממש UI לפרופיל עם ניצחונות והפסדים. — המונים מציגים 0; שמירת הנתונים ועריכת פרטים (מוצגת כלא זמינה) עדיין פתוחות.
- [x] לממש UI להגדרות צליל, רטט ותגובות. — שמירת ההעדפות עדיין פתוחה.
- [x] לממש מסך `איך משחקים?`.

### משחק ברשת

- [x] לממש בחירת קטגוריות מרובה. — קטגוריות מהשרת במשחק ברשת ובחדר פרטי.
- [x] לממש חיפוש שחקנים עם 4–8 מקומות. — שחקנים שנמצאו, מקומות ריקים וטיימר.
- [x] לממש חוקי ההתחלה: 30 שניות מהשחקן הרביעי, 5 שניות מהשישי ועד 8. — `server/internal/matchmaking`.
- [x] לממש ביטול חיפוש. — כולל ביטול בניתוק.
- [x] לממש מצב שאין התאמה לקטגוריות. — אחרי 2 דקות, עם `בחירת קטגוריות מחדש` ו־`ניסיון נוסף`.
- [x] לממש `משחק נוסף` והשלמת שחקנים חסרים. — חוזרים לחיפוש יחד, ושחקנים חדשים משלימים.

### מנוע המשחק

- [x] לממש יצירת משחק, בחירת מילה ובחירת מתחזה. — מומש בשרת ומחובר באפליקציה לחדרים פרטיים.
- [x] לממש חשיפת תפקיד לאזרח ולמתחזה. — מומש בשרת ומחובר באפליקציה לחדרים פרטיים. המתחזה אינו מקבל את המילה.
- [x] לממש אישור תפקיד והתחלת סבב. — מומש בשרת ומחובר באפליקציה לחדרים פרטיים.
- [x] לממש סדר תורות אקראי. — מומש בשרת ומחובר באפליקציה לחדרים פרטיים.
- [x] לממש טיימר רמז של 15 שניות. — מומש בשרת ומחובר באפליקציה לחדרים פרטיים.
- [x] לממש שליחה והצגה מיידית של רמזים לפי תור. — מומש בשרת ומחובר באפליקציה לחדרים פרטיים.
- [ ] לממש חסימת מילה סודית, רמז כפול ותוכן לא ראוי. — חסימת המילה הסודית (לאזרחים בלבד), רמז כפול וניחוש מנורמל מומשו במנוע ובאפליקציה מול מקרי בדיקה משותפים; מילון התוכן הלא ראוי פתוח.
- [x] לממש אימוג'ים והודעות מובנות ללא הגבלת כמות. — הרשימה שאושרה, מהשרת (מחובר באפליקציה לחדרים פרטיים).
- [x] לממש הצבעה של 20 שניות ושינוי בחירה עד הסיום. — מומש בשרת ומחובר באפליקציה לחדרים פרטיים.
- [x] לממש הימנעות אוטומטית לשחקן שלא הצביע. — מומש בשרת ומחובר באפליקציה לחדרים פרטיים.
- [x] לממש הצבעה חוזרת ותיקו שני שמעניק ניצחון למתחזה. — מומש בשרת ומחובר באפליקציה לחדרים פרטיים.
- [x] לממש ניחוש מילה למתחזה בתוך 15 שניות. — מומש בשרת ומחובר באפליקציה לחדרים פרטיים.
- [x] לממש חישוב ניצחון והפסד לפי החוקים. — מומש בשרת ומחובר באפליקציה לחדרים פרטיים.
- [x] לממש מסכי תוצאה וחלוקת הצבעות. — מומש בשרת ומחובר באפליקציה לחדרים פרטיים.
- [ ] לממש שמירת ניצחונות והפסדים.

### חדרים פרטיים

- [x] לממש יצירת חדר עם עד 8 שחקנים. — `POST /v1/rooms` עם קטגוריות מהשרת, מהאפליקציה.
- [ ] לממש קוד חדר בן שש ספרות ושיתוף. — קוד ייחודי מהשרת והעתקה באפליקציה; שיתוף מערכת פתוח.
- [x] לממש הצטרפות לחדר וטיפול בקוד לא תקין. — מהאפליקציה, עם שגיאות השרת בתוך המסך.
- [x] לממש לובי פרטי ורשימת שחקנים. — לובי חי מ־`room.state`, כולל שחקנים מנותקים.
- [x] לממש נעילת הגדרות לאחר הצטרפות שחקן נוסף. — נאכף בשרת; באפליקציה ההגדרות נבחרות רק ביצירת החדר.
- [x] לממש הסרת שחקן על ידי המנהל. — `room.kick` מהלובי; השחקן שהוסר חוזר למסך הבית עם הודעה.
- [x] לממש העברת מנהל לאחר 30 שניות או יציאה. — בשרת; בלובי מוצגים טיימר המתנה למנהל והודעת העברת ניהול.
- [x] לממש התחלת משחק מארבעה שחקנים. — `room.start` מהאפליקציה; עד שיוחלט מילון התוכן הלא ראוי השרת מתחיל משחק רק עם `IMPOSTER_DEV_POLICY=1`.
- [x] לממש השארת החדר פתוח למשחק נוסף. — `game.playAgain` מחזיר ללובי.

### ניתוקים ומקרי קצה

- [x] לממש חיבור מחדש ושחזור המסך הפעיל. — האפליקציה מתחברת מחדש אוטומטית ומציגה את ה־Snapshot האחרון עם הודעת `מתחברים מחדש...`.
- [x] לממש טיימר חיבור מחדש של 30 שניות בזמן תור. — מומש במנוע ומחובר באפליקציה לחדרים פרטיים.
- [x] לממש דילוג על תור בניתוק הראשון והשני. — מומש במנוע ומחובר באפליקציה לחדרים פרטיים.
- [x] לממש הוצאה והפסד בניתוק השלישי. — במנוע; באפליקציה מסך הוצאה (27).
- [x] לממש הפסד ביציאה יזומה. — מומש במנוע ומחובר באפליקציה לחדרים פרטיים.
- [x] לממש המשך משחק עם שלושה שחקנים. — מומש במנוע ומחובר באפליקציה לחדרים פרטיים.
- [x] לממש סיום משחק כאשר נשארים פחות משלושה. — במנוע; באפליקציה תוצאת `המשחק הופסק`.
- [x] לממש טיפול בקריסת שרת ללא רישום הפסד. — המצב בזיכרון בלבד, והאפליקציה מציגה את מסך התקלה (29) כשהשרת איבד את ה־session ויוצרת session חדש. `game.aborted` לא נדרש כל עוד אין שמירה.
- [x] למנוע מאירוע רשת ישן להחזיר משחק לשלב קודם. — השרת שולח `stateVersion` מונוטוני והאפליקציה מתעלמת מגרסה ישנה.

## P1 — בדיקות ואיכות

- [x] לכתוב Unit Tests לחוקי המשחק ול־State Machine.
- [ ] לכתוב Integration Tests ל־API, Realtime ומסד הנתונים. — קיימות בדיקות REST ו־WebSocket עם חיבורים אמיתיים; אין עדיין מסד נתונים.
- [x] לכתוב End-to-End Tests לזרימת משחק ברשת. — `app/test/e2e`: ארבעה sessions מוצאים זה את זה ומתחילים משחק מול השרת, ב־CI.
- [x] לכתוב End-to-End Tests לזרימת חדר פרטי. — `app/test/e2e`: ארבעה sessions של האפליקציה משחקים משחק מלא מול השרת האמיתי, ב־CI. בדיקות ה־UI עצמן מול שרת מדומה.
- [ ] לבדוק ניתוקים, חיבור מחדש, הודעות כפולות ואירועים שמגיעים בסדר שגוי. — מכוסה בבדיקות השרת ובבדיקות האפליקציה מול שרת מדומה (חיבור שנפל, session שאבד, Snapshot ישן); בדיקה על מכשיר ורשת אמיתית פתוחה.
- [ ] לבצע Load Test ל־Matchmaking ולמשחקים מקבילים.
- [ ] לבדוק את כל 29 המצבים מול האפיון.
- [ ] לבדוק RTL ונגישות.
- [ ] לבדוק רוחב 320px ומכשירי Android ו־iPhone נפוצים. — ה־widget tests רצים ברוחב 320px; בדיקה על מכשירים עדיין פתוחה.
- [ ] לבדוק רשת איטית, מעבר Wi-Fi/סלולר והחזרת האפליקציה מהרקע.
- [ ] לבדוק את מילון התוכן הלא ראוי וכללי נרמול העברית.
- [ ] לבצע בדיקת אבטחה בסיסית ל־API, חדרים ו־Rate limits.
- [ ] לוודא שאין איבוד משחק או רישום הפסד שגוי לאחר תקלה.

## P2 — הכנה להשקה

- [ ] לבחור שם Package/Bundle ID. — כרגע `com.example.imposter_il` זמני ב־Android וב־iOS.
- [ ] להכין תנאי שימוש ומדיניות פרטיות.
- [ ] להגדיר כתובת תמיכה וערוץ קבלת פידבק.
- [ ] לפתוח ולהגדיר חשבונות Apple Developer ו־Google Play Console.
- [ ] להכין אייקון, screenshots וטקסטים לחנויות.
- [ ] להקים סביבת Production וניטור התראות.
- [ ] לבצע Alpha פנימית.
- [ ] לבצע Beta סגורה עם שחקנים אמיתיים.
- [ ] לאסוף ולתקן בעיות מה־Beta.
- [ ] לבצע בדיקת עומס סופית לפני השקה.
- [ ] להגיש את האפליקציה ל־App Store ול־Google Play.
- [ ] לעקוב אחרי crashes, זמני Matchmaking ונטישות לאחר ההשקה.

## מאוחר יותר — מחוץ ל־MVP

- [ ] לתכנן מצב שאלות.
- [ ] לבחון מערכת חברים.
- [ ] לבחון דירוג תחרותי עולמי.
- [ ] לבחון חנות, פרסומות או חבילות בתשלום.
- [ ] לבחון צ'אט חופשי או קולי רק בהחלטת מוצר חדשה ומפורשת.
- [ ] להוסיף קטגוריות, מילים ואיורים חדשים בהתאם לנתוני שימוש.

## יומן עדכונים

- 2026-09-15 — תיקוני Bugbot: מינימום Flutter עלה ל־3.44.0 בגלל תבניות Android/iOS, ונוספו ל־CI build של Android ושל iOS; פתיחה מחדש של האפליקציה באמצע משחק חוזרת אליו; יציאה מחדר, ממשחק ומחיפוש מחכה לאישור השרת; שחקנים שבוחרים `משחק נוסף` נשארים באותה קבוצה גם כשבחרו קטגוריות שונות; ובחירת הצבעה מתאפסת בהצבעה חוזרת.

- 2026-09-15 — הוחלטו ומומשו כללי Matchmaking: 30 שניות מהשחקן הרביעי, ספירה של 5 שניות מהשישי (גם ב־8) שאינה נעצרת בביטול כל עוד נשארו 4, התחלה מחדש מתחת ל־4, ו־`לא נמצא משחק מתאים` אחרי 2 דקות. השרת מקבץ שחקנים לפי קטגוריה משותפת, `משחק נוסף` מחזיר לחיפוש יחד, והאפליקציה מחוברת למשחק ברשת. נתוני הדמה והעתק כללי המילים באפליקציה הוסרו.

- 2026-09-15 — האפליקציה מחוברת לשרת לחדרים פרטיים: מעטפות Android/iOS (מזהה זמני), כניסה כאורח ושמירת זהות, קטגוריות ותגובות מהשרת, יצירה והצטרפות, לובי חי ומשחק מלא לפי Snapshots וטיימרים של השרת, חיבור מחדש, הוצאה ותקלה בשרת. נוספו בדיקות Widget מול שרת מדומה ובדיקת End-to-End מול השרת ב־CI. משחק ברשת נשאר על נתוני דמה עד Matchmaking.

- 2026-09-15 — עדכון כללי של המשימות והמסמכים למצב הנוכחי: אבן הדרך, סעיפי הושלם, משימות ניתוקים ובדיקות שכבר מכוסות בשרת, ותיקוני ניסוח ב־`architecture.md`, `protocol.md` ו־README.

- 2026-09-15 — המשחק רץ מקצה לקצה ב־WebSocket: `room.start`, כל הודעות `game.*`, `game.state` מסונן לכל שחקן, `game.reaction`, טיימרי משחק, שחקן שהוצא רואה את המשחק, ו־`game.playAgain`. אושרה מנה ראשונה של תוכן (6 קטגוריות × 20 מילים) ונוסף `GET /v1/categories`. אושרה רשימת התגובות (6 אימוג'ים ו־4 הודעות) ונוסף `GET /v1/reactions`. מילון התוכן הלא ראוי עדיין פתוח, ולכן משחק מתחיל רק עם `IMPOSTER_DEV_POLICY=1`.

- 2026-09-15 — נוסף `GET /v1/ws` ללובי פרטי: מעטפת v1, idempotency, ping, החלפת חיבור, `session.state`, `room.state`, `room.kicked`, `room.updateSettings`, `room.kick`, `room.leave`, ניתוק וחיבור מחדש וטיימר העברת מנהל. התלות החיצונית הראשונה בשרת: `github.com/coder/websocket`. התחלת משחק והודעות `game.*` בשלב הבא.

- 2026-09-15 — נוסף `server/internal/api`: `POST /v1/sessions`, `PATCH /v1/sessions/me`, `POST /v1/rooms` ו־`POST /v1/rooms/join`, בזיכרון, עם קודי חדר ייחודיים. הוחלט שכינוי ריק נדחה ונדרשים לפחות 2 תווים; אורך מרבי ותווים מותרים עדיין פתוחים.

- 2026-09-15 — נוסף `server/internal/room`: State Machine של חדר פרטי עם קוד בן שש ספרות, הגדרות וננעלות, הצטרפות, הסרה, התחלת משחק עם שחקנים מחוברים ומשך רמז לפי ההגדרות, חזרה ללובי בסוף משחק, והעברת מנהל ב־`host_timeout`, `host_left` ו־`host_removed`. הוחלט ששחקן שהוסר יכול להצטרף שוב, ושחברים מנותקים נכללים במשחק בלי ניתוק נספר.

- 2026-09-15 — הוחלט: רמז של המתחזה אינו נבדק מול המילה הסודית, ומילים מושוות אחרי נרמול עברית (ניקוד, סימנים ואותיות סופיות), עם התעלמות מעד 3 אותיות שימוש ברמז כפול ובניחוש ובכתיב מדויק. המנוע והאפליקציה עודכנו ונבדקים מול `word_rules.json`.

- 2026-09-15 — תיקוני Review ל־Flutter Prototype: בדיקת פורמט אמיתית ב־CI, מינימום Flutter 3.27.0 שנבדק ב־CI לצד stable, כפתור חזרה במסכים שאינם מסכי משחק, הסרת התחלת משחק ידנית מחיפוש שחקנים, ספירות דמה שמקדמות את ה־Prototype ומופרדות מטיימרי השרת, קוד חדר בספרות בלבד, בדיקת רמז כפול מול רמזים שנחשפו בלבד, הסתרת המילה מהמתחזה, תיקון סימון מנהל החדר, השבתת פעולות שעדיין אינן ממומשות, ו־widget tests לזרימות. נתוני הדמה מרוכזים ב־`app/lib/demo/`.

- 2026-09-14 — נוסף בסיס Flutter צבעוני ו־RTL: כניסה, בית, פרופיל, הגדרות, הוראות, קטגוריות, Matchmaking, שלבי המשחק, חדר פרטי ומצבי שגיאה; 12 האווטארים ו־11 האילוסטרציות חוברו כ־assets ונוסף CI. המסכים הם Prototype מקומי ועדיין אינם מחוברים לשרת.

- 2026-09-14 — נוצר קובץ המעקב; סומנו האפיון, ה־Wireframes ונכסי האיור שכבר הושלמו.
- 2026-09-14 — נקבעו Flutter, Go ו־monorepo; נוספו מסמכי ארכיטקטורה ופרוטוקול, שלד שרת, מנוע משחק ראשון עם בדיקות ו־CI. משימות המימוש המלאות תחת "מנוע המשחק" נשארות פתוחות עד חיבורן לרשת ולאפליקציה.
- 2026-09-14 — אושרו מקרי הקצה שעלו בתכנון המנוע: כל ניתוק נספר, הצבעה חוזרת של 15 שניות, תוצאה כשהמתחזה יוצא, הצבעה ללא קולות, קול של מנותק ותגובות עד סוף המשחק. המנוע, הבדיקות והמסמכים עודכנו. טיימר חשיפת התפקיד עדיין פתוח.
- 2026-09-14 — נקבעו 10 שניות לחשיפת תפקיד; המנוע והמסמכים עודכנו, ונוספה משימת עיצוב לטיימר במסכים 7–8.
