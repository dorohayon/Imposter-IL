# משימות הפרויקט

עודכן לאחרונה: 23 בספטמבר 2026

מקור המעקב המרכזי של `מי המתחזה?`. חוקי המוצר וההחלטות המלאות נמצאים ב־`docs/`.

הקובץ נכתב מחדש מול מצב הקוד בפועל. הגרסה הקודמת הלכה לאיבוד מול המציאות —
היא עדיין טענה שחסימת תוכן נדחתה, שאין Rate limiting ושספק ה־Hosting פתוח,
בזמן ששלושתם מומשו ונפרסו. ההיסטוריה המלאה נמצאת ב־git; כאן נשאר מה שפתוח.

## כללי מעקב

- `[ ]` פתוח, `[x]` הושלם.
- משימה שמסומנת **חוסם** מונעת הגשה לחנויות עד שתושלם.
- בכל השלמת משימה יש לסמן אותה כאן באותו commit.
- החלטת מוצר חדשה מתועדת גם ב־`docs/decisions.md`.
- אין להתחיל משימות של מצב שאלות לפני השלמת ה־MVP של מצב המילים.

## איפה הפרויקט עומד

המשחק שלם ופועל מקצה לקצה, ברשת, בחדרים פרטיים ובמכשיר אחד, על שרת שרץ
בפרודקשן (Cloud Run, `deploy/setup-cloudrun.sh`). מה שכבר עומד: מנוע משחק
בשרת עם בדיקות, WebSocket עם חיבור מחדש, Matchmaking, המסכים באפליקציה מול
העיצוב, עברית ו־RTL, Rate limiting, ניקוי משאבים, Metrics ולוגים, שער גרסת
לקוח, חסימת תוכן (Blocked Words V1), דיווח והסתרה, תנאי שימוש ומדיניות פרטיות
עם שער הסכמה ודפים ציבוריים, קישור הזמנה לחדר, ובוטים שכותבים רמזים לפי
הקטגוריה ומצביעים לפי קריאת הסבב.

מאז העדכון הקודם נוספו: המשחק במכשיר אחד (3–12 שחקנים, ללא שרת), סבבי
רמזים־והצבעה מרובים עם הדחה וצופים, שלב `elimination_reveal` שמוצג גם ברשת
וממתין לאישור של כל השולחן או ל־5 שניות, מסך הרמזים המחודש סביב לוח המשתתפים,
ומסך חיפוש השחקנים המחודש בשלושת מצבי הקבוצה.

**מצב הפרודקשן כרגע:** `MIN_CLIENT_BUILD=3`, `STAGING_BOTS=5`. בענף הזה
`clientBuild = 4` ב־`app/lib/data/server.dart` ו־`0.1.0+4` ב־`pubspec.yaml`
(מודל ההכנסות). אין להעלות את `MIN_CLIENT_BUILD` ל־4 לפני שהגרסה בחנויות.

מה שחסר הוא כמעט כולו סביב ההגשה לחנויות ולא סביב המשחק עצמו.

## חוסם הגשה לחנויות

אלה בלתי הפיכים או איטיים, ולכן הם המסלול הקריטי.

- [ ] **חוסם — מזהה אפליקציה סופי.** כרגע `com.example.imposter_il` ב־Android
      ו־`com.example.imposterIl` ב־iOS. החנויות דוחות `com.example`, וב־Android
      המזהה נעול לצמיתות אחרי הפרסום הראשון ולא ניתן לשנותו לעולם.
- [ ] **חוסם — מפתח חתימה לגרסת הפצה.** `android/app/build.gradle.kts` עדיין
      חותם גרסאות release במפתח ה־debug (יש שם `TODO`). גם המפתח נעול לצמיתות:
      אובדן שלו מונע עדכונים למי שכבר התקין.
- [ ] **חוסם — חשבונות מפתח.** Apple Developer (99$ לשנה, כולל אימות זהות
      שלוקח ימים) ו־Google Play (25$ חד־פעמי).
- [ ] **חוסם — לכבות `STAGING_BOTS` בפרודקשן** לפני שחקנים אמיתיים, אחרת
      זרים ישובצו למשחק עם בוטים.
- [ ] לקבוע מספר גרסה להשקה. `pubspec.yaml` על `0.1.0+1`; ה־CI אוכף
      ש־`clientBuild` ב־Dart שווה למספר ה־build, ולכן השניים זזים יחד.
- [ ] למלא App Store Privacy ו־Google Play Data Safety לפי מה שמומש בפועל
      (`docs/legal.md` מפרט מה להצהיר).
- [ ] להצהיר על קהל יעד 13+ בשתי החנויות, ולוודא שדירוג הגיל עקבי עם תשובות
      ה־UGC (`docs/decisions.md`, סעיף משפטי וחנויות).
- [ ] **אסור** להדביק את תנאי השימוש בשדה ה־EULA של App Store Connect. הם
      תנאי בית ולא EULA של Apple; שדה ריק מחיל את ה־EULA הסטנדרטי. הסבר מלא
      ב־`docs/legal.md`.

## מודל הכנסות

מומש בענף `feat/monetization` (`docs/monetization.md`, העיצוב
`design/claude/Imposter IL Monetization.dc.html`).

- [x] שלוש קטגוריות חינמיות מוגדרות מרחוק; שאר הקטגוריות גלויות ונעולות בשלושת
      המצבים: ברשת, בחדר פרטי ובמכשיר אחד.
- [x] חלון רכישה אחד (P01–P12) בלי מסך חנות: קטגוריה לתמיד, פרימיום חודשי,
      פרימיום לכל החיים, מחירים מקומיים מהחנות, גילוי חידוש, שחזור, קישורים
      לתנאים ולפרטיות, ומצבי טעינה, עיבוד, ביטול, שגיאה, המתנה לאישור ושחזור.
- [x] מקור אמת בחנות, שחזור שקט בהפעלה ובחזרה מהרקע, מטמון לא־מקוון, החזר כספי,
      ביטול ופקיעת מנוי; סגירת עסקה מיידית (Play מחזיר אחרי 3 ימים).
- [x] השרת: `GET /v1/config`, `POST /v1/entitlements`, אימות JWS של Apple ו־token
      של Google, ו־`category_locked` בחדרים וב־matchmaking תחת `serverEnforcement`.
- [x] באנר אדפטיבי במסכים שבעיצוב, מודעה במסך מלא אחרי משחק שהושלם בלבד, UMP,
      טקסט ATT, ושורות `שחזור רכישות` ו־`העדפות פרטיות לפרסומות` בהגדרות.
- [x] תנאי שימוש ומדיניות פרטיות 1.1 באפליקציה ובדפים הציבוריים; כל התקנה תתבקש
      לאשר מחדש.
- [ ] **חוסם — מוצרים בחנויות:** `category_<id>` לכל קטגוריה שאינה חינמית,
      `premium_monthly` (קבוצת מנויים, חודש, להפעיל Billing Grace Period)
      ו־`premium_lifetime`, בשתי החנויות, עם מחירים לכל מדינה.
- [ ] **חוסם — AdMob:** חשבון, שתי אפליקציות, מזהי אפליקציה במקום מזהי הבדיקה
      (`-PadmobAppId=…` ל־Android, `ADMOB_APP_ID` ב־`ios/Flutter/*.xcconfig`),
      ומזהי יחידות ב־`MONETIZATION_CONFIG` (`ads.units`). בלעדיהם גרסת release
      אינה מציגה פרסומות — לא נשברת.
- [ ] **חוסם — הודעת הסכמה ב־AdMob** (Privacy & messaging): GDPR לאיחוד, בריטניה
      ושווייץ, והסבר IDFA ל־iOS.
- [ ] להוסיף את רשימת ה־SKAdNetwork המלאה של Google ל־`Info.plist` (כרגע רק
      `cstr6suwn9`).
- [x] אתר ציבורי ב־`https://imposteril.github.io` (בית, תמיכה, פרטיות, תנאים,
      `app-ads.txt`), נבנה מ־`site/` ומתפרסם ב־`.github/workflows/site.yml`.
- [ ] להכניס ל־`site/app-ads.txt` את השורה עם מזהה ה־publisher של AdMob.
- [ ] בדפי החנויות: אתר מפתח `https://imposteril.github.io`, מדיניות פרטיות
      `/privacy/`, ו־Support URL ב־App Store Connect `/support/`.
- [ ] להפעיל אכיפה בשרת: `APPLE_BUNDLE_ID`, `GOOGLE_PLAY_PACKAGE`,
      `GOOGLE_PLAY_SERVICE_ACCOUNT` (Secret Manager), `MIN_CLIENT_BUILD=4`,
      ואז `serverEnforcement: true` (`deploy/README.md`).
- [ ] לעדכן App Privacy ו־Data Safety עם מה ש־AdMob אוסף, ו־"Tracking: Yes"
      ב־App Store Connect (`docs/legal.md`).
- [ ] קישור ל־EULA הסטנדרטי של Apple בתיאור האפליקציה ב־App Store.
- [ ] בדיקה על מכשיר בסביבת Sandbox / License testers: קנייה, ביטול, החזר,
      Ask to Buy, מנוי מואץ שפג, שחזור במכשיר שני, מצב טיסה, ואיך נראה באנר אמיתי
      ב־320px.
- [x] בניית iOS עם `google_mobile_ads` ו־`in_app_purchase` (Swift Package
      Manager): עברה ב־CI על Flutter 3.44.0.
- [ ] החלטה פתוחה: פרסומות לגיל לא ידוע (`docs/open-decisions.md`).
- [ ] להעביר לפרויקט העיצוב את המצבים שמומשו ואינם בו: ממתין לאישור, שחזור של
      משהו אחר, שחזור שנכשל, מחירים שלא נטענו, רכישות כבויות, שורת השחזור ושורת
      העדפות הפרסומות בהגדרות. וגם: על באנר אמיתי לא מונחת תווית `מודעה` — AdMob
      אוסר להניח דבר מעל המודעה, והיא נושאת סימון משלה.

## לפני השקה, לא חוסם העלאה

- [ ] Crash Reporting. אין כרגע; ה־SDK היחיד של צד שלישי הוא Google Mobile Ads.
      **שימו לב לקשר:** סעיף 7 במדיניות הפרטיות מבטיח
      שאין SDK צד שלישי ל־analytics או crash reporting, ולכן הוספת Crashlytics
      מחייבת עדכון של הסעיף, של טופס Data Safety ושל כרטיס App Privacy —
      באותה גרסה, לא אחריה.
- [ ] לוגו סופי, App Icon ו־Splash Screen. כרגע זמניים (דמות הזכוכית המגדלת,
      `app/branding`).
- [ ] צלילים. מסומנים `בקרוב` בהגדרות; הרטט מומש.
- [ ] Monitoring והתרעות מעבר ל־`/metrics` וללוגים. התרעת התקציב קיימת בקונסולה
      אבל ה־CLI אינו יכול לאמת אותה — Cloud Billing Budget API כבוי בפרויקט.
- [ ] לאשר ולהקפיא Design V1.

## משחק במכשיר אחד

- [x] אפיון UX, Wireframes ושני איורים חדשים למצב מקומי (`docs/local-game-design.md`).
- [x] Claude Design: עיצוב High Fidelity ו־Prototype
      (`design/claude/Imposter IL Local Game.dc.html`, `Imposter IL Local Prototype.dc.html`).
- [x] מימוש מנוע משחק מקומי באפליקציה ללא שרת, כולל שמירת משחק פעיל במכשיר
      (`app/lib/local/`).
- [x] בדיקות פרטיות בהעברת המכשיר, חזרה מה־background ומשחק מלא של 3–12 שחקנים
      (`app/test/local_game_test.dart`, `local_game_widget_test.dart`).
- [ ] **מסך 15ד — הכרזת תיקו במשחק ברשת: מעוצב, והוחלט לא לממש.** נבנה פעם אחת
      ובוטל בבחירה — במשחק ברשת התיקו כבר מוצג כבאנר על מסך ההצבעה החוזרת, וזה
      הספיק. במכשיר אחד ההכרזה נחוצה כי אין מסך לכל שחקן.
      אם חוזרים לזה: צריך שלב `tie_break` סמכותי בשרת עם מועד סיום, ורק אחריו
      `runoff_voting` עם 15 שניות מלאות, אחרת ההכרזה מוצגת מעל הצבעה ששעונה
      כבר רץ (`docs/tie-screen-design.md`).
- [ ] לתקן בעיצוב: מסכי התוצאה של המשחק המקומי כתוב בהם `חזרה למסך הראשי`, והנוסח
      המאושר ב־`CLAUDE.md` הוא `חזרה למסך הבית`. המימוש משתמש במאושר;
      `docs/local-game-design.md` תוקן, וייצוא Claude Design עדיין לא.

## פערים בין העיצוב למימוש

- [x] איור ואפיון מסך הכרזה על תיקו לפני הצבעה חוזרת, כולל וריאציה למשחק מקומי
      ותיקו חוזר ללא הדחה (`docs/tie-screen-design.md`).
- [ ] להוסיף את מסך ההכרזה ל־Claude Design ולמימוש Flutter. ברשת נדרש שלב
      מעבר בשרת לפני `runoff_voting` כדי ש־15 השניות יתחילו רק אחרי ההכרזה;
      במכשיר אחד יש לשמור על מסך הפרטיות לפני הצבעה חוזרת.

נמצאו בסקירת העיצוב מול הכללים המאושרים. כל אחד הוא תיקון טקסט או מצב יחיד.

- [x] מסך 05: פוצל לשלושת מצבי השרת (`searching`, `waiting_for_more`,
      `countdown`), סונכרן מ־Claude Design ומומש. ההבטחה השגויה על התחלה
      מיידית ב־8 ירדה מהנוסח.
- [ ] מסך 01: בעיצוב כתוב 2–12 תווים לכינוי; הכלל הוא 2–18, וכך אוכפים השרת
      והאפליקציה.
- [ ] מסך 24: הצלילים מוצגים כפעילים ובפועל הם `בקרוב`.
- [x] מסך 24: שורת הגרסה מוצגת (`מי המתחזה? · גרסה 1.0`,
      `app/lib/screens/secondary_screens.dart`).
- [ ] תגובות: בעיצוב 🤨 😂 🔍 👏 ו־`חשוד מאוד`, `מה הקשר?`, `רמז טוב`.
      המאושר: 😂 🤔 👀 😮 👏 🙄 ו־`רמז טוב!`, `זה מחשיד`, `לא השתכנעתי`,
      `מה הקשר?`.
- [ ] מסך 02 (כינוי חסום) עוצב לעתיד; כעת החסימה מומשה, ולכן צריך לוודא
      שהמסך תואם את ההתנהגות בפועל.

## תוכן

- [ ] לבצע בדיקת איכות אנושית למילים: עברית, כפילויות, רמת קושי והתאמה
      לקטגוריה.
- [ ] להחליט אם 6 קטגוריות × 20 מילים מספיקות להשקה (`docs/open-decisions.md`).
- [ ] איור קטן ושקוף לכל קטגוריה, ושילובו בכרטיסי הקטגוריה ובמערכת העיצוב.
      **חוסם** עד שרשימת הקטגוריות תיסגר.
- [ ] כל מילה חדשה שתתווסף חייבת 5–7 רמזי אזרח ב־`bot_hints.json`, אחרת
      הבוטים נופלים לרמזי הקטגוריה הרחבים עבורה (`docs/bot-hints.md`).

## סבבים מרובים

מומש: המשחק ממשיך בסבבי רמזים־והצבעה עד שאחד הצדדים מנצח (`docs/architecture.md`).

- [x] לעדכן את העיצוב למצבים החדשים. מסך הרמזים עוצב מחדש סביב לוח המשתתפים
      (`design/claude/Imposter IL Clue Screen.dc.html`, מסכים C01–C08) וכולל את
      מצב הצופה ואת ספירת הסבב והתור. המפריד בין סבבים ותג הסבב ירדו יחד עם
      לוח הרמזים הישן.
- [x] **נפרס:** `MIN_CLIENT_BUILD=3` (עלה ל־3 עם `elimination_reveal`).
      התקנה ישנה מקבלת מסך עדכון ולא מסך שבור.
- [x] **הוכרע:** שתי הצבעות ברצף בלי אף הצבעה מבטלות את המשחק, בלי ניצחון ובלי הפסד לאיש.
- [ ] לבדוק על מכשיר משחק ארוך של 8 שחקנים: כמה סבבים נדרשים עד הכרעה, והאם זה מרגיש ארוך מדי.

## נפתח בעבודה האחרונה

- [ ] העיצוב ב־Claude Design החזיר במסך 13 טיימר של 20 שניות להצבעה חוזרת
      ואת הנוסח `תיקו נוסף מעניק ניצחון למתחזה`. שניהם סותרים את
      `docs/decisions.md` (15 שניות; תיקו חוזר אינו מדיח איש). העותק המקומי
      תוקן לפי סדר העדיפות שב־`CLAUDE.md`, ויש לתקן גם בפרויקט העיצוב כדי
      שהייצוא הבא לא יחזיר את השגיאה.
- [ ] להריץ את `docs/design-sync-brief.md` מול פרויקט Claude Design, ולייצא
      חזרה ל־`design/claude/`. הוא מרכז את כל מה שהעיצוב עדיין מפגר אחריו.
- [x] סונכרן מ־Claude Design (23 בספטמבר): עיצוב מודל ההכנסות, Prototype רכישה,
      כרטיס רמז קצר בשורה אחת ומסך ניחוש המתחזה עם הרמזים של שאר השחקנים —
      שני האחרונים מומשו. הייצוא החזיר **שוב** את הרגרסיה במסך 13 (20 שניות,
      `תיקו נוסף מעניק ניצחון למתחזה`); העותק המקומי תוקן שוב.
- [ ] בדיקה על מכשיר של שלב ההדחה עם שולחן מלא: האם 5 שניות מספיקות כשכולם
      צריכים לאשר, ומה קורה כששחקן מנותק בדיוק בשלב הזה.

## החלטות מוצר פתוחות

מתוך `docs/open-decisions.md`:

- [ ] האם הטיות שמשנות את סוף המילה (`בננה` מול `בננות`) נחשבות לחשיפת המילה,
      לרמז כפול או לניחוש נכון.
- [ ] מה נחשב "מילה אחת": מקף, מקף עברי או גרש. **עלה בפועל** בבניית מאגר
      רמזי הבוטים — ההנחיה להימנע מגרש יצרה `גונגל` ו־`ציפס` שנראים כשגיאות
      כתיב, והוחזרו ל־`ג'ונגל` ו־`צ'יפס`.
- [ ] אילו תווים מותרים בכינוי. האורך הוחלט: 2 עד 18.
- [ ] נוסח סופי להודעת שגיאה בקריסת שרת.

## בדיקות שנותרו

- [ ] בדיקה על מכשירים אמיתיים נפוצים ב־Android וב־iPhone, מול העיצוב.
- [ ] רשת איטית, מעבר Wi-Fi/סלולר והחזרת האפליקציה מהרקע.
- [ ] Load Test מעבר למדידה שבוצעה (240 שחקנים, 30 משחקים במקביל, p99 של
      14.4ms; `server/cmd/loadbot`).
- [ ] בדיקת אבטחה בסיסית ל־API, לחדרים ול־Rate limits.
- [ ] נגישות: ניגודיות, גודל אזורי לחיצה וקוראי מסך.
- [ ] בדיקת קישור ההזמנה מקצה לקצה על מכשיר — לחיצה מווטסאפ אל תוך האפליקציה.
      לא ניתן לבדיקה ב־CI.

## אחרי ההשקה

- [ ] App Links ב־https במקום דף הביניים של ההזמנה. דורש את טביעת האצבע של
      מפתח החתימה ל־Android ואת ה־Team ID ל־iOS, ולכן חסום עד שיהיו.
- [x] כתובות המסמכים המשפטיים אינן תלויות עוד בשרת המשחק: הן ב־
      `https://imposteril.github.io`, והשרת מפנה אליהן.
- [ ] בוטים שמגיבים לפי מה שנאמר ולא באקראי, ואז שקילת התגובות כאות בהצבעה.
- [ ] מצב שאלות. לא לפני שה־MVP של מצב המילים הושק.
